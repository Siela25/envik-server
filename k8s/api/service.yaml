apiVersion: v1
kind: Service
metadata:
  name: envik-api-service
  namespace: envik
spec:
  selector:
    app: envik-api
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort
  